[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
testpaths = tests
markers =
    slow: exercises the real embedding model (slow, no network; opt in with -m slow)
addopts = -m "not slow"
